# VK Mini App / Hacker News / ID 51914828

## 🗂️ Предустановленные библиотеки

Это мини-приложение от ВКонтакте, построенное на `React` и `npx @vkontakte/create-vk-mini-app`. В качестве API выступает [HackerNews API](https://github.com/HackerNews/API), на котором разработан основной визуал (лента последних 100 новостей, а также комментарии к ним).

## 🚀 Запуск мини приложения

1. Скачайте репозиторий
2. Запустите команду `yarn install` (если до этого пользовались `npm`, необходимо выполнить команду `npm install --global yarn`)
3. После скачивания зависимостей запустите приложение командой `yarn start`. Приложение запустится локально по адресу: http://localhost:3000/